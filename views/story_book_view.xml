<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="story_book_form" model="ir.ui.view">
        <field name="name">story.book.form</field>
        <field name="model">story.book</field>
        <field name="arch" type="xml">
            <form string="Story Book">
                <sheet>
                    <group>
                        <field name="title"/>
                        <field name="preview"/>
                        <field name="tag_ids"/>
                    </group>
                    <notebbok>
                        <page string="Content">
                            <field name="content_ids">
                                <tree editable="bottom">
                                    <field name="order_seq"/>
                                    <field name="paragraph"/>
                                </tree>
                            </field>
                        </page>
                    </notebbok>
                </sheet>
            </form>
        </field>
    </record>

    <record id="story_book_tree" model="ir.ui.view">
        <field name="name">story.book.tree</field>
        <field name="model">story.book</field>
        <field name="arch" type="xml">
            <tree name="Story Book" editable="bottom">
                <field name="title"/>
                <field name="preview"/>
                <field name="tag_ids"/>
            </tree>
        </field>
    </record>

    <record id="story_book_action" model="ir.actions.act_window">
        <field name="name">Story Book</field>
        <field name="res_model">story.book</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="story_book_menu" name="Story Book" parent="story_menu" sequence="10"/>

</odoo>
